<div class="col-xs-12 col-sm-6">
    <div class="card border">
        <img class="card-img-top" src="{{icon}}" alt="{{name}}">
        <div class="card-body">
            <h5 class="card-title">{{name}} <span class="small">by {{author}}</span></h5>
            <p class="card-text">{{{description}}}</p>
        </div>

        <div class="card-footer">
            <div class="d-flex justify-content-between">
                <a
                    type="button"
                    class="btn btn-link"
                    target="_blank"
                    href="{{homepage}}"
                >View Demo&nbsp;&nbsp;<i class="fa fa-external-link-alt"></i></a>
                <button
                    type="button"
                    class="btn btn-primary"
                    data-install-control="install-theme"
                    data-theme-code="{{code}}"
                >Install Theme
                </button>
            </div>
        </div>
    </div>
</div>
